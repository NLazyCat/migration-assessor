# Implementation Plans

Generated by the improve skill on 2026-07-19. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 000 | Establish CI baseline by fixing pre-existing clippy, fmt, and test fixture issues | P0 | M | none | DONE |
| 001 | Add CI/CD workflows and release gates | P0 | S | none | DONE |
| 002 | Align VALID_LANGUAGES with supported SourceLanguage variants | P0 | S | none | DONE |
| 003 | Introduce ProjectContext for command data loading | P1 | M | 001, 002 | DONE |
| 004 | Shard reference indexes and drop monolithic forward/reverse JSON | P1 | M | 003 | DONE |
| 005 | Add incremental analysis cache | P2 | L | 003 | SKIPPED |
| 006 | Restructure output directories and add manifest.json | P2 | L | 003, 004 | DONE |
| 007 | Enrich graph data with degrees and directory hierarchy | P2 | S | 006 | DONE |
| 008 | Parallelize symbol and reference extraction | P2 | M | 003 | DONE |
| 009 | Add NDJSON optional output format | P2 | S | 006 | DONE |

## Dependency notes

- 003 (ProjectContext) depends on 001/002 because those plans touch config and CI; landing them first keeps the shared context layer stable.
- 004 depends on 003 because reference loading must go through the new context layer.
- 006 depends on 003/004 because output path changes must be centralized in ProjectContext and the sharded references are part of the new layout.
- 007 depends on 006 because it changes the serialized graph shape, which is part of the output restructure.
- 008 can run in parallel with 004/005/006 once 003 is done, but it touches the same analysis pipeline; serialize after 006 if you want one clean integration test pass.
- 009 depends on 006 because NDJSON is an alternate serialization of the restructured outputs.

## Findings considered and rejected

- `benchmark.yml` / `nightly.yml`: deferred. They add CI maintenance before the test/lint baseline is proven; add after `ci.yml` is green for a week.
- File-level placeholder mirroring (`mirror_source_structure`): kept as-is for now. While it wastes disk I/O, removing it affects the migration folder contract; revisit as a separate design decision.
- `detect_source_repo` only scanning immediate subdirectories: documented as known constraint, not fixed in this batch because it changes the CLI invocation contract.
