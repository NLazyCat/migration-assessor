{
  "exports": [
    {
      "generics": [],
      "kind": "interface",
      "line_range": [
        22,
        25
      ],
      "name": "McpStubServerHandle",
      "params": [],
      "partial_analysis": false,
      "signature": "export interface McpStubServerHandle { ... }"
    },
    {
      "generics": [],
      "kind": "property",
      "line_range": [
        23,
        23
      ],
      "name": "url",
      "params": [],
      "partial_analysis": false,
      "return_type": ": string",
      "signature": "url: : string"
    },
    {
      "generics": [],
      "kind": "function",
      "line_range": [
        46,
        86
      ],
      "name": "startMcpStubServer",
      "params": [
        {
          "name": "input",
          "optional": false,
          "type": "{\r\n  marker: string;\r\n  /**\r\n   * When true, every HTTP request must include a non-empty\r\n   * `Authorization: Bearer <token>` header. Unauth'd requests get a\r\n   * `401 Unauthorized`. Used by smokes that exercise OAuth-backed MCP\r\n   * connections; without this gate the smoke can't prove the access\r\n   * token actually reached MCP. Defaults to false to keep the legacy\r\n   * non-auth smoke path available.\r\n   */\r\n  requireBearer?: boolean;\r\n}"
        }
      ],
      "partial_analysis": false,
      "return_type": "Promise<McpStubServerHandle>",
      "signature": "export function startMcpStubServer(input: {\r\n  marker: string;\r\n  /**\r\n   * When true, every HTTP request must include a non-empty\r\n   * `Authorization: Bearer <token>` header. Unauth'd requests get a\r\n   * `401 Unauthorized`. Used by smokes that exercise OAuth-backed MCP\r\n   * connections; without this gate the smoke can't prove the access\r\n   * token actually reached MCP. Defaults to false to keep the legacy\r\n   * non-auth smoke path available.\r\n   */\r\n  requireBearer?: boolean;\r\n}) -> Promise<McpStubServerHandle>"
    }
  ],
  "module": "packages/eve/test/tui-client/lib/mcp-stub-server.ts"
}